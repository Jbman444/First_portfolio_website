---
const { platform, username } = Astro.props;

const label = String(platform ?? "");
const key = label.toLowerCase();

let url = "";

if (key === "linkedin") {
  url = "https://www.linkedin.com/in/" + username;
} else if (key === "github") {
  url = "https://github.com/" + username;
} else if (key === "email") {
  url = "mailto:" + username;
} else if (key === "resume") {
  url = String(username ?? "");
}
---

<a
  href={url}
  class={`social-btn ${
    key === "email" ? "email-btn" : key === "resume" ? "resume-btn" : ""
  }`}
  target={key === "email" ? undefined : "_blank"}
  rel={key === "email" ? undefined : "noopener noreferrer"}
>
  {label}
</a>

<style>
  .social-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.6rem 0.95rem;
    border-radius: 999px;
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    border: 1px solid #cbd5e1;
    background: white;
    color: #111827;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.08);
    transition: all 0.15s ease;
  }

  .social-btn:hover,
  .social-btn:focus {
    transform: translateY(-1px);
    background: #f1f5f9;
  }

  /* ✅ EMAIL ALWAYS RED */
  .email-btn {
    background: #ff7f66 !important;
    color: white !important;
    border: none !important;
  }

  .email-btn:hover,
  .email-btn:focus {
    background: #ff6a4d !important;
  }

  /* DARK MODE BASE */
  :global(html.dark) .social-btn {
    background: white;
    color: black;
    border-color: white;
  }

  /* ❗ REMOVE or weaken this override effect */
  :global(html.dark) .social-btn:hover,
  :global(html.dark) .social-btn:focus {
    background: #e5e7eb;
  }

  /* ✅ FORCE EMAIL TO IGNORE DARK MODE */
  :global(html.dark) .email-btn {
    background: #ff7f66 !important;
    color: white !important;
  }

  :global(html.dark) .email-btn:hover,
  :global(html.dark) .email-btn:focus {
    background: #ff6a4d !important;
  }
</style>